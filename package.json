{
  "name": "browser-bot",
  "version": "1.0.0",
  "description": "general browser bot that can be configured with a config file",
  "dependencies": {
    "selenium-webdriver": "^4.4.0",
    "typescript": "^4.8.3"
  },
  "devDependencies": {
    "@types/selenium-webdriver": "^4.1.5",
    "jest": "^29.2.1",
    "ts-jest": "^29.0.3"
  },
  "scripts": {
    "test": "jest",
    "lint:fix": "npx prettier --write lib/",
    "prepack": "babel --out-dir dist src --extensions '.ts,.tsx'"
  },
  "author": "",
  "license": "ISC"
}
